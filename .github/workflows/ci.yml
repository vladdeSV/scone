name: CI

on:
  push:
    branches: [ develop ]
  pull_request:
    branches: [ develop ]

jobs:
  build:
    runs-on: ${{ matrix.operating-system }}
    strategy:
      matrix:
        operating-system: [ubuntu-latest, macos-latest, windows-latest]

    steps:
      - uses: actions/checkout@v2
      - uses: mihails-strasuns/setup-dlang@v0.5.0

      - name: Build
        run: dub build --config=ci

      - name: Test
        run: dub test --config=ci
